{
  "name": "safepath-root",
  "version": "1.0.0",
  "scripts": {
    "build": "cd client && npm install && npm run build && cd .. && mkdir -p dist && cp -rf client/dist/* dist/"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.0",
    "mongoose": "^7.0.3",
    "node-fetch": "^2.6.9",
    "nodemailer": "^8.0.10"
  }
}
